// src/ai/responses.js — AI response generation placeholder
export async function generateResponse(messages, env) {
  // Will use Workers AI in Wave 2
  return {
    text: 'AI response placeholder',
    model: 'placeholder'
  };
}
