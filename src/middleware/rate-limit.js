// src/middleware/rate-limit.js — Rate limiting placeholder
export async function checkRateLimit(sessionId, env) {
  // Will implement rate limiting in Wave 2
  return { allowed: true, remaining: 10 };
}
