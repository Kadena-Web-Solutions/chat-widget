// src/middleware/session.js — Session middleware placeholder
export async function getOrCreateSession(request, env) {
  // Will create/retrieve sessions in Wave 2
  return { sessionId: crypto.randomUUID() };
}
