// src/index.js — Main entry point placeholder
// Will be populated in Wave 2 with full routing

export default {
  async fetch(request, env, ctx) {
    return new Response('chat-widget-worker placeholder', { status: 200 });
  }
};
